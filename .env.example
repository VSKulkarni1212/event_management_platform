# Database Configuration
DB_HOST=localhost
DB_NAME=event_platform
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Application Settings
APP_ENV=development
APP_URL=http://localhost/event_management_platform
APP_NAME=Event Management Platform

# Session Settings
SESSION_LIFETIME=3600
SESSION_NAME=EVENT_PLATFORM_SESSION

# File Upload Settings
MAX_FILE_SIZE=5242880
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif

# Email Settings (for PHP mail() function)
MAIL_FROM_ADDRESS=noreply@eventplatform.local
MAIL_FROM_NAME=Event Platform

# Security Settings
CSRF_TOKEN_NAME=csrf_token
PASSWORD_MIN_LENGTH=8
